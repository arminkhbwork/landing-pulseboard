##
# Docker ports
# - PORT: internal container port the app listens on (keep 3000 in most cases)
# - HOST_PORT: host machine port mapped to the container (change this to avoid conflicts)
PORT=3000
HOST_PORT=3000

##
# Public (safe to expose)
#
# Example: https://pulseboard.arminkhabazha.com
NEXT_PUBLIC_SITE_URL=

# 1 = allow indexing, 0 = noindex + disallow via robots.txt
NEXT_PUBLIC_ALLOW_INDEXING=1

##
# Contact / links
#
# Replace later with your real repo URL
NEXT_PUBLIC_GITHUB_REPO_URL=https://github.com/your-handle/landing-pulseboard
